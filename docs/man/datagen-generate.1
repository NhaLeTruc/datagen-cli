.nh
.TH "DATAGEN" "1" "Nov 2025" "datagen-cli" "User Commands"

.SH NAME
datagen-generate - Generate PostgreSQL dump from JSON schema


.SH SYNOPSIS
\fBdatagen generate [flags]\fP


.SH DESCRIPTION
Generate a PostgreSQL dump file from a JSON schema definition.

.PP
The schema defines tables, columns, and data generation rules.
Output is a SQL file compatible with PostgreSQL.

.PP
You can use a pre-built template with --template or provide a custom schema with --input.


.SH OPTIONS
\fB-f\fP, \fB--format\fP="sql"
	output format: sql (INSERT statements), copy (COPY format)

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for generate

.PP
\fB-i\fP, \fB--input\fP=""
	input schema file (default: stdin)

.PP
\fB-o\fP, \fB--output\fP=""
	output SQL file (default: stdout)

.PP
\fB--param\fP=[]
	override template parameters (format: key=value)

.PP
\fB-s\fP, \fB--seed\fP=0
	random seed for deterministic generation

.PP
\fB--template\fP=""
	use pre-built template (ecommerce, saas, healthcare, finance)


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB-c\fP, \fB--config\fP=""
	config file (default is .datagen.yaml)


.SH EXAMPLE
.EX
  # Generate from custom schema (SQL format by default)
  datagen generate -i schema.json -o dump.sql

  # Generate from template
  datagen generate --template ecommerce -o dump.sql

  # Generate with COPY format
  datagen generate -i schema.json -o dump.sql --format copy

  # Generate from template with custom parameters
  datagen generate --template saas --param tenants=500 -o dump.sql

  # Generate with deterministic seed
  datagen generate -i schema.json -o dump.sql --seed 12345
.EE


.SH SEE ALSO
\fBdatagen(1)\fP


.SH HISTORY
16-Nov-2025 Auto generated by spf13/cobra
